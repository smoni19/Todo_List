CREATE TABLE todo_lists(id SERIAL PRIMARY KEY, name VARCHAR(200), category VARCHAR(100), created TIMESTAMP WITH TIME ZONE, archived BOOLEAN NOT NULL DEFAULT FALSE, account_id INTEGER REFERENCES users (id));