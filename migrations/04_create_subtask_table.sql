CREATE TABLE subtasks(id SERIAL PRIMARY KEY, details VARCHAR(200), deadline TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP, completed BOOLEAN NOT NULL DEFAULT FALSE, task_id INTEGER REFERENCES tasks (id));